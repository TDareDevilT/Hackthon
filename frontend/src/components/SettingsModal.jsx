import {AnimatePresence,motion} from 'framer-motion';
import {X,Globe,PlayCircle,UserRound} from 'lucide-react';
import {translate as tr} from '../lib/i18n';
import LanguagePicker from './LanguagePicker';
export default function SettingsModal({open,onClose,language,onLanguage,onTutorial,onProfile}){return <AnimatePresence>{open&&<motion.div className="modal-backdrop" initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}}><motion.div className="settings-modal" initial={{y:20,opacity:0}} animate={{y:0,opacity:1}} exit={{y:10,opacity:0}} role="dialog" aria-modal="true"><button className="icon-btn close" onClick={onClose} aria-label={tr(language,'close')}><X/></button><span className="eyebrow"><Globe size={15}/> FIELD SETTINGS</span><h2>{tr(language,'settingsTitle')}</h2><div className="setting-row"><div><b>{tr(language,'profile')}</b><p>{tr(language,'profileHint')}</p></div><button className="secondary" onClick={()=>{onClose();onProfile?.()}}><UserRound size={16}/>{tr(language,'editProfile')}</button></div><div className="setting-row"><div><b>{tr(language,'language')}</b><p>{tr(language,'languageHint')}</p></div><LanguagePicker language={language} onLanguage={onLanguage}/></div><div className="setting-row"><div><b>{tr(language,'tutorial')}</b><p>{tr(language,'tutorialStart')}</p></div><button className="secondary" onClick={()=>{onClose();onTutorial()}}><PlayCircle size={16}/>{tr(language,'replay')}</button></div></motion.div></motion.div>}</AnimatePresence>}
