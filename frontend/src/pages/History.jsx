import {useEffect,useState} from 'react';import {api} from '../lib/api';import {CalendarDays,Trash2,ArrowRight} from 'lucide-react';import {translate as tr} from '../lib/i18n';
export default function History({onOpen,language}){const [items,setItems]=useState([]),[error,setError]=useState('');useEffect(()=>{api('/api/reports').then(setItems).catch(e=>setError(e.message))},[]);const del=async id=>{try{await api(`/api/reports/${id}`,{method:'DELETE'});setItems(x=>x.filter(i=>i.id!==id))}catch(e){setError(e.message)}};return <div className="history-page"><div className="page-title"><div><span className="eyebrow">FIELD MEMORY</span><h1>{tr(language,'reportHistory')}</h1><p>{tr(language,'historyText')}</p></div></div>{error&&<div className="error">{error}</div>}{!items.length&&!error?<div className="empty"><CalendarDays size={34}/><h3>{tr(language,'noReports')}</h3><p>{tr(language,'historyEmpty')}</p></div>:<div className="history-list">{items.map(i=><article className="history-row" key={i.id}><div><span>{new Date(i.created_at).toLocaleString()}</span><h3>{i.crop} · {i.crop_stage}</h3><p>{i.farm_name}{i.location?` · ${i.location}`:''}</p></div><div className="history-actions"><button onClick={()=>onOpen({report:i.report,mode:'REAL'})}><ArrowRight size={17}/></button><button onClick={()=>del(i.id)} aria-label="Delete report"><Trash2 size={17}/></button></div></article>)}</div>}</div>}
