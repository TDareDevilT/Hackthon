import {useState} from 'react';import {Leaf,Lock,Eye,EyeOff,ArrowRight} from 'lucide-react';import {supabase} from '../lib/supabase';import SiteFooter from '../components/SiteFooter';
export default function ResetPassword(){const [password,setPassword]=useState(''),[confirm,setConfirm]=useState(''),[show,setShow]=useState(false),[busy,setBusy]=useState(false),[done,setDone]=useState(false),[error,setError]=useState('');const submit=async e=>{e.preventDefault();setError('');if(password.length<8)return setError('Password must be at least 8 characters.');if(password!==confirm)return setError('Passwords do not match.');setBusy(true);const {error}=await supabase.auth.updateUser({password});setBusy(false);if(error)return setError(error.message);setDone(true);setTimeout(()=>{location.href='/';},1200)};return <div className="reset-page"><div className="reset-card"><div className="mini-brand"><Leaf/> Climate Aware</div><h1>Set a new password.</h1><p>Choose a new password for your climate-ready fieldspace.</p>{done?<div className="notice">Password updated. Returning to your fieldspace…</div>:<form onSubmit={submit}><label><span><Lock size={14}/> New password</span><div className="password-wrap"><input value={password} onChange={e=>setPassword(e.target.value)} type={show?'text':'password'} required minLength="8"/><button type="button" onClick={()=>setShow(v=>!v)} aria-label={show?'Hide password':'Show password'}>{show?<EyeOff size={17}/>:<Eye size={17}/>}</button></div></label><label><span><Lock size={14}/> Confirm password</span><div className="password-wrap"><input value={confirm} onChange={e=>setConfirm(e.target.value)} type={show?'text':'password'} required minLength="8"/><button type="button" onClick={()=>setShow(v=>!v)} aria-label={show?'Hide password':'Show password'}>{show?<EyeOff size={17}/>:<Eye size={17}/>}</button></div></label>{error&&<div className="error">{error}</div>}<button className="primary wide" disabled={busy}>{busy?'Updating…':'Update password'}<ArrowRight size={17}/></button></form>}</div><SiteFooter/></div>}
